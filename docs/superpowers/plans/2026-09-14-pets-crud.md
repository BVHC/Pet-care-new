# Pets v1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Customer quản lý Pet của chính mình — AddPet / ViewPet (list+detail) / UpdatePet với guard Primary Owner (RULE-04-01→04-03).

**Architecture:** Modular monolith `com.petcare.module.pet/` copy khung `module/auth`: Controller → Service → Repository, DTO record + MapStruct, `@Transactional` ở Service, owner-check server-side, Outbox `PetAdded` cùng tx. Không FSM handler (PetStatus không có FSM riêng — prose invariant RULE-04-11; v1 khóa `status=ACTIVE`).

**Tech Stack:** Spring Boot 3.5.15 / Java 21 / Spring Data JPA + PostgreSQL + Flyway / MapStruct 1.6.3 / springdoc 2.8.11 / spring-boot-starter-validation / Testcontainers Postgres (`@ServiceConnection`).

**Spec:** `docs/01-business-operations.md §4`, `docs/02-business-rules.md RULE-04-01→04-03 (+04-11 rút gọn)`, `docs/06-erd.md §3.2/§3.3 bảng pets`, `docs/api/customer-pet-v1.md #1-4`, `docs/convention/backend/01-package-structure.md + 02-layering-and-dto.md + 04-exception-handling.md + 07-transaction-management.md`.

## Global Constraints

- Boot 3.5.15 / Java 21 là authoritative (`BE/pom.xml`) — không lên Boot 4, không thêm dependency mới.
- Package `com.petcare.module.pet.{controller,service,repository,entity,dto,mapper}` (số ít như `auth/iam`); `platform/` không phụ thuộc ngược `module/*`.
- Cấm import trực tiếp entity/repository module khác — check owner qua `UserProvisioningService.findById/findByAccountId` (pattern `AuthServiceImpl.login/refresh`).
- Entity `extends platform.model.BaseEntity` (id UUID, createdAt/updatedAt/createdBy/updatedBy/deletedAt/version); Lombok `@Getter @Setter @NoArgsConstructor`, không `@Data`/`@Builder` trên entity.
- DTO là Java record + Bean Validation; mapping bằng MapStruct `componentModel="spring"`; cấm trả Entity qua Controller; Controller trả `ApiResponse.created/ok` + `PageResponse.of` cho list.
- Exception chỉ dùng 5 base `platform/exception/*` (`BusinessRuleViolationException(ruleId,msg)→400`, `ResourceNotFoundException→404`, `AccessDeniedScopeException→403`, `InvalidStateTransitionException→409`, `ConcurrencyConflictException→409`); message nhúng RULE-ID; không tạo `module/pet/exception/*`.
- `@Transactional` ở Service (write mặc định, read `readOnly=true`); không ở Controller/Repository.
- Không sửa `V1__init_schema.sql`; migration mới `V4__pets_audit_columns.sql`.
- OUT v1 (không làm): `POST /pets/{id}/transfer` (RULE-04-10 auto-revoke), `GET /pets/search`, `status=DECEASED/TRANSFERRED`, caregiver invitation/accept/reject/revoke, receptionist tạo hộ.
- Ponytail full: reuse pattern Auth/IAM, shortest diff, không abstraction thừa. TDD iron law: không production code khi chưa có test đỏ.

---

### Task 1: Migration audit cho `pets`

**Files:**
- Create: `BE/src/main/resources/db/migration/V4__pets_audit_columns.sql`
- Test: khởi context IT (Task 6) — Flyway migrate thành công, không test riêng.

**Interfaces:**
- Consumes: `V1__init_schema.sql:290-303` (bảng `pets` hiện thiếu 5 cột audit), `BaseEntity.java` (updated_at/created_by/updated_by/deleted_at/version).
- Produces: bảng `pets` khớp `BaseEntity` cho Task 2 persist thành công.

- [ ] **Step 1: Viết migration**

```sql
-- V4: bổ sung audit columns cho pets khớp platform.model.BaseEntity (accounts/users đã đủ ở V1).
ALTER TABLE pets
    ADD COLUMN updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    ADD COLUMN created_by UUID REFERENCES accounts(id),
    ADD COLUMN updated_by UUID REFERENCES accounts(id),
    ADD COLUMN deleted_at TIMESTAMPTZ,
    ADD COLUMN version BIGINT NOT NULL DEFAULT 0;
```

- [ ] **Step 2: Verify migrate**

Run: `mvn -pl BE flyway:migrate -Dflyway.url=jdbc:postgresql://localhost:5432/petcare -Dflyway.user=postgres -Dflyway.password=postgres` (hoặc chạy IT Task 6, context load = migrate pass)
Expected: PASS, `pets` có đủ 5 cột.

- [ ] **Step 3: Commit**

```bash
git add BE/src/main/resources/db/migration/V4__pets_audit_columns.sql
git commit -m "feat(pet): add pets audit columns V4"
```

---

### Task 2: `Pet` entity + repository (TDD slice)

**Files:**
- Create: `BE/src/main/java/com/petcare/module/pet/entity/Pet.java`
- Create: `BE/src/main/java/com/petcare/module/pet/repository/PetRepository.java`
- Test: `BE/src/test/java/com/petcare/module/pet/repository/PetRepositoryTest.java`

**Interfaces:**
- Consumes: `BaseEntity`, `platform.enums.PetStatus (ACTIVE,DECEASED,TRANSFERRED)`, bảng `pets` sau Task 1.
- Produces: `Pet`, `PetRepository.findByOwnerId(UUID)` cho Task 4.

- [ ] **Step 1: Write the failing test**

```java
@DataJpaTest
@AutoConfigureTestDatabase(replace = AutoConfigureTestDatabase.Replace.NONE)
class PetRepositoryTest {
  @Autowired PetRepository repo;

  @Test
  void saveAndFindByOwnerId() {
    UUID owner = UUID.randomUUID();
    Pet pet = new Pet(owner, "Milo", "DOG");
    repo.save(pet);
    assertThat(repo.findByOwnerId(owner)).hasSize(1);
  }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `mvn -pl BE test -Dtest=PetRepositoryTest`
Expected: FAIL — `Pet`/`PetRepository` not defined. Testcontainers Postgres real DB (không H2), theo `spring-boot/references/testing-slices-persistence.md` + `testcontainers-wiring.md` (`@ServiceConnection` nếu cấu hình chung của repo).

- [ ] **Step 3: Write minimal implementation**

```java
@Entity @Table(name = "pets")
@Getter @Setter @NoArgsConstructor
public class Pet extends BaseEntity {
  @Column(name = "owner_id", nullable = false)
  private UUID ownerId;
  @Column(name = "name", nullable = false, length = 100)
  private String name;
  @Column(name = "species", nullable = false, length = 50)
  private String species = "DOG";
  @Column(name = "breed", length = 100)
  private String breed;
  @Column(name = "gender", nullable = false, length = 10)
  private String gender = "UNKNOWN";
  @Column(name = "date_of_birth")
  private LocalDate dateOfBirth;
  @Column(name = "weight_kg", precision = 5, scale = 2)
  private BigDecimal weightKg;
  @Column(name = "microchip_number", length = 50)
  private String microchipNumber;
  @Column(name = "avatar_url", length = 255)
  private String avatarUrl;
  @Enumerated(EnumType.STRING)
  @JdbcTypeCode(SqlTypes.NAMED_ENUM)
  @Column(name = "status", nullable = false)
  private PetStatus status = PetStatus.ACTIVE;

  public Pet(UUID ownerId, String name, String species) {
    this.ownerId = ownerId; this.name = name; this.species = species;
    this.status = PetStatus.ACTIVE;
  }
}
```

```java
public interface PetRepository extends JpaRepository<Pet, UUID> {
  List<Pet> findByOwnerId(UUID ownerId);
}
```

FK giữ dạng `UUID` thô (không `@ManyToOne` — giữ Bounded Context, như `User.accountId`). `species/gender` là VARCHAR → String thường; chỉ `status` dùng `NAMED_ENUM` (copy `Account.status`).

- [ ] **Step 4: Run test to verify it passes**

Run: `mvn -pl BE test -Dtest=PetRepositoryTest`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add BE/src/main/java/com/petcare/module/pet/entity BE/src/main/java/com/petcare/module/pet/repository BE/src/test/java/com/petcare/module/pet/repository
git commit -m "feat(pet): add Pet entity and repository"
```

---

### Task 3: DTO record + MapStruct mapper

**Files:**
- Create: `BE/src/main/java/com/petcare/module/pet/dto/CreatePetRequest.java`
- Create: `BE/src/main/java/com/petcare/module/pet/dto/UpdatePetRequest.java`
- Create: `BE/src/main/java/com/petcare/module/pet/dto/PetResponse.java`
- Create: `BE/src/main/java/com/petcare/module/pet/mapper/PetMapper.java`
- Test: `BE/src/test/java/com/petcare/module/pet/mapper/PetMapperTest.java`

**Interfaces:**
- Consumes: `Pet` (Task 2).
- Produces: `CreatePetRequest/UpdatePetRequest/PetResponse`, `PetMapper.toResponse(Pet)` cho Task 4/5.

- [ ] **Step 1: Write the failing test**

```java
class PetMapperTest {
  private final PetMapper mapper = Mappers.getMapper(PetMapper.class);

  @Test
  void toResponse_mapsIdAndOwner() {
    Pet pet = new Pet(UUID.randomUUID(), "Milo", "DOG");
    PetResponse res = mapper.toResponse(pet);
    assertThat(res.name()).isEqualTo("Milo");
    assertThat(res.status()).isEqualTo(PetStatus.ACTIVE);
  }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `mvn -pl BE test -Dtest=PetMapperTest`
Expected: FAIL — mapper/DTO chưa tồn tại.

- [ ] **Step 3: Write minimal implementation**

```java
public record CreatePetRequest(
  @NotBlank @Size(max = 100) String name,
  @NotBlank String species,
  @Size(max = 100) String breed,
  String gender,
  LocalDate dateOfBirth,
  BigDecimal weightKg,
  @Size(max = 50) String microchipNumber,
  @Size(max = 255) String avatarUrl) {}

public record UpdatePetRequest(
  @Size(max = 100) String name,
  String species, @Size(max = 100) String breed, String gender,
  LocalDate dateOfBirth, BigDecimal weightKg,
  @Size(max = 50) String microchipNumber,
  @Size(max = 255) String avatarUrl) {}
// Không có ownerId/status — server gán owner từ principal, khóa status=ACTIVE ở v1.

public record PetResponse(UUID id, UUID ownerId, String name, String species,
  String breed, String gender, LocalDate dateOfBirth, BigDecimal weightKg,
  String microchipNumber, String avatarUrl, PetStatus status) {}

@Mapper(componentModel = "spring")
public interface PetMapper {
  PetResponse toResponse(Pet entity);
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `mvn -pl BE test -Dtest=PetMapperTest`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add BE/src/main/java/com/petcare/module/pet/dto BE/src/main/java/com/petcare/module/pet/mapper BE/src/test/java/com/petcare/module/pet/mapper
git commit -m "feat(pet): add pet DTOs and mapper"
```

---

### Task 4: `PetService` — create/read/update + owner-guard (TDD lõi)

**Files:**
- Create: `BE/src/main/java/com/petcare/module/pet/service/PetService.java`
- Create: `BE/src/main/java/com/petcare/module/pet/service/PetServiceImpl.java`
- Test: `BE/src/test/java/com/petcare/module/pet/service/PetServiceImplTest.java`

**Interfaces:**
- Consumes: `PetRepository`, `PetMapper`, `UserProvisioningService.findById(UUID)` (verify owner tồn tại), `OutboxEventRepository`, `UserPrincipal.getUserId()`.
- Produces: `create(me, req): PetResponse`, `list(me, pageable): Page<PetResponse>`, `detail(me, id): PetResponse`, `update(me, id, req): PetResponse`.

- [ ] **Step 1: Write the failing tests** (mỗi test 1 behavior, mock repo/provisioning như `AuthServiceImplTest`)

```java
@ExtendWith(MockitoExtension.class)
class PetServiceImplTest {
  @Mock PetRepository pets; @Mock UserProvisioningService users;
  @Mock OutboxEventRepository outbox; @Mock PetMapper mapper;
  @InjectMocks PetServiceImpl svc;

  @Test
  void create_setsOwnerToSelf_andEmitsPetAdded() {
    UUID me = UUID.randomUUID();
    when(users.findById(me)).thenReturn(new User(UUID.randomUUID(), "A"));
    when(pets.save(any())).thenAnswer(i -> i.getArgument(0));
    PetResponse out = svc.create(me, new CreatePetRequest("Milo", "DOG", null, "MALE", null, null, null, null));
    assertThat(out.ownerId()).isEqualTo(me);
    verify(outbox).save(argThat(e -> e.getEventType().equals("PetAdded")));
  }

  @Test
  void detail_otherOwnersPet_forbidden() {
    Pet pet = new Pet(UUID.randomUUID(), "Milo", "DOG");
    when(pets.findById(any())).thenReturn(Optional.of(pet));
    assertThatThrownBy(() -> svc.detail(UUID.randomUUID(), pet.getId()))
      .isInstanceOf(AccessDeniedScopeException.class);
  }

  @Test
  void update_rejectsStatusChange() {
    // v1 không cho đổi status qua PATCH — RULE-04-11
    assertThatThrownBy(() -> svc.updateWithStatus(UUID.randomUUID(), UUID.randomUUID(), "DECEASED"))
      .isInstanceOf(BusinessRuleViolationException.class);
  }
}
```

(`updateWithStatus` chỉ là test minh họa guard — implement thực tế: `UpdatePetRequest` không có field status nên test này kiểm tra Service ném `RULE-04-11` nếu phát hiện status khác ACTIVE trong entity path; nếu giữ DTO không-status thì bỏ test này và giữ 2 test trên + 1 test `update_nonOwner_forbidden`. Implementer chọn 1 trong 2, không để test chết.)

- [ ] **Step 2: Run tests to verify they fail**

Run: `mvn -pl BE test -Dtest=PetServiceImplTest`
Expected: FAIL — `PetService` chưa tồn tại.

- [ ] **Step 3: Write minimal implementation** (copy khung `AuthServiceImpl`)

```java
@Service @RequiredArgsConstructor
public class PetServiceImpl implements PetService {
  private final PetRepository pets;
  private final PetMapper mapper;
  private final UserProvisioningService users;
  private final OutboxEventRepository outbox;

  @Override @Transactional
  public PetResponse create(UUID me, CreatePetRequest req) {
    users.findById(me); // RULE-04-01: owner phải tồn tại; 404 nếu không
    Pet pet = new Pet(me, req.name(), req.species());
    pet.setBreed(req.breed()); pet.setGender(req.gender() == null ? "UNKNOWN" : req.gender());
    pet.setDateOfBirth(req.dateOfBirth()); pet.setWeightKg(req.weightKg());
    pet.setMicrochipNumber(req.microchipNumber()); pet.setAvatarUrl(req.avatarUrl());
    try { pet = pets.save(pet); }
    catch (DataIntegrityViolationException ex) {
      throw new BusinessRuleViolationException("RULE-04-01", "Chủ sở hữu không hợp lệ");
    }
    OutboxEvent e = new OutboxEvent();
    e.setAggregateType("Pet"); e.setAggregateId(pet.getId().toString());
    e.setEventType("PetAdded");
    e.setPayload("{\"petId\":\"" + pet.getId() + "\",\"ownerId\":\"" + me + "\"}");
    outbox.save(e);
    return mapper.toResponse(pet);
  }

  @Override @Transactional(readOnly = true)
  public PetResponse detail(UUID me, UUID id) {
    Pet pet = pets.findById(id).orElseThrow(() -> new ResourceNotFoundException("Pet", id));
    if (!pet.getOwnerId().equals(me)) throw new AccessDeniedScopeException("PET", "OWNER");
    return mapper.toResponse(pet);
  }

  @Override @Transactional(readOnly = true)
  public Page<PetResponse> list(UUID me, Pageable pageable) {
    return pets.findByOwnerId(me, pageable).map(mapper::toResponse);
  }

  @Override @Transactional
  public PetResponse update(UUID me, UUID id, UpdatePetRequest req) {
    Pet pet = pets.findById(id).orElseThrow(() -> new ResourceNotFoundException("Pet", id));
    if (!pet.getOwnerId().equals(me)) throw new AccessDeniedScopeException("PET", "OWNER");
    if (pet.getStatus() != PetStatus.ACTIVE)
      throw new BusinessRuleViolationException("RULE-04-11", "Pet không ở trạng thái cho phép cập nhật");
    if (req.name() != null) pet.setName(req.name());
    if (req.species() != null) pet.setSpecies(req.species());
    if (req.breed() != null) pet.setBreed(req.breed());
    if (req.gender() != null) pet.setGender(req.gender());
    if (req.dateOfBirth() != null) pet.setDateOfBirth(req.dateOfBirth());
    if (req.weightKg() != null) pet.setWeightKg(req.weightKg());
    if (req.microchipNumber() != null) pet.setMicrochipNumber(req.microchipNumber());
    if (req.avatarUrl() != null) pet.setAvatarUrl(req.avatarUrl());
    try { pet = pets.save(pet); }
    catch (ObjectOptimisticLockingFailureException ex) { throw new ConcurrencyConflictException("Pet", id); }
    return mapper.toResponse(pet);
  }
}
```

Cần thêm `Page<Pet> findByOwnerId(UUID, Pageable)` vào Repository ở Task này (sửa nhỏ Task 2, ghi rõ trong commit). `AccessDeniedScopeException(required,actual)` copy chữ ký từ platform.

- [ ] **Step 4: Run tests to verify they pass**

Run: `mvn -pl BE test -Dtest=PetServiceImplTest`
Expected: PASS + toàn bộ suite cũ vẫn xanh.

- [ ] **Step 5: Commit**

```bash
git add BE/src/main/java/com/petcare/module/pet/service BE/src/test/java/com/petcare/module/pet/service
git commit -m "feat(pet): add PetService with owner guard"
```

---

### Task 5: `PetController` (web slice)

**Files:**
- Create: `BE/src/main/java/com/petcare/module/pet/controller/PetController.java`
- Test: `BE/src/test/java/com/petcare/module/pet/controller/PetControllerTest.java`

**Interfaces:**
- Consumes: `PetService`, `PetMapper` (không cần — Service đã trả Response), `UserPrincipal`.
- Produces: `POST /api/pets → 201`, `GET /api/pets → 200 Page`, `GET /api/pets/{id} → 200`, `PATCH /api/pets/{id} → 200`.

- [ ] **Step 1: Write the failing test**

```java
@WebMvcTest(PetController.class)
@Import({SecurityConfig.class, GlobalExceptionHandler.class})
class PetControllerTest {
  @Autowired MockMvc mvc; @MockBean PetService svc;

  @Test
  @WithMockUser
  void create_invalidName_400() throws Exception {
    mvc.perform(post("/api/pets").contentType(APPLICATION_JSON)
        .content("{\"name\":\"\",\"species\":\"DOG\"}"))
      .andExpect(status().isBadRequest());
  }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `mvn -pl BE test -Dtest=PetControllerTest`
Expected: FAIL — controller chưa tồn tại (404/filter khác 400 mong đợi).

- [ ] **Step 3: Write minimal implementation**

```java
@RestController @RequestMapping("/api/pets") @RequiredArgsConstructor
public class PetController {
  private final PetService svc;

  @PostMapping
  @PreAuthorize("isAuthenticated()")
  public ResponseEntity<ApiResponse<PetResponse>> create(
      @AuthenticationPrincipal UserPrincipal me, @Valid @RequestBody CreatePetRequest req) {
    return ResponseEntity.status(HttpStatus.CREATED)
      .body(ApiResponse.created(svc.create(me.getUserId(), req), "success"));
  }

  @GetMapping
  @PreAuthorize("isAuthenticated()")
  public ResponseEntity<ApiResponse<PageResponse<PetResponse>>> list(
      @AuthenticationPrincipal UserPrincipal me, Pageable pageable) {
    return ResponseEntity.ok(ApiResponse.ok(PageResponse.of(svc.list(me, pageable))));
  }

  @GetMapping("/{id}")
  @PreAuthorize("isAuthenticated()")
  public ResponseEntity<ApiResponse<PetResponse>> detail(
      @AuthenticationPrincipal UserPrincipal me, @PathVariable UUID id) {
    return ResponseEntity.ok(ApiResponse.ok(svc.detail(me.getUserId(), id)));
  }

  @PatchMapping("/{id}")
  @PreAuthorize("isAuthenticated()")
  public ResponseEntity<ApiResponse<PetResponse>> update(
      @AuthenticationPrincipal UserPrincipal me, @PathVariable UUID id,
      @Valid @RequestBody UpdatePetRequest req) {
    return ResponseEntity.ok(ApiResponse.ok(svc.update(me.getUserId(), id, req)));
  }
}
```

`ApiResponse/PageResponse` copy cách dùng từ `AuthController` + `architecture/system-overview.md §5`. Không logic RULE-ID ở Controller.

- [ ] **Step 4: Run test to verify it passes**

Run: `mvn -pl BE test -Dtest=PetControllerTest`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add BE/src/main/java/com/petcare/module/pet/controller BE/src/test/java/com/petcare/module/pet/controller
git commit -m "feat(pet): add PetController CRUD"
```

---

### Task 6: E2E `PetFlowIT` + verify toàn suite

**Files:**
- Create: `BE/src/test/java/com/petcare/module/pet/PetFlowIT.java`

**Interfaces:**
- Consumes: toàn bộ Task 1-5 + running Postgres Testcontainers + JWT auth (tái dùng setup `AuthFlowIT`).

- [ ] **Step 1: Write the failing test**

```java
// @SpringBootTest + Testcontainers (copy khung AuthFlowIT 242 dòng):
// register → verify OTP → login lấy accessToken →
// POST /api/pets (201, ownerId == me) → GET /api/pets (thấy 1) →
// GET /api/pets/{id} (200) → PATCH đổi name (200) →
// login user B GET /api/pets/{id} của A (403) → PATCH của B (403).
// Mỗi assert là 1 test trong class IT.
class PetFlowIT { /* ... */ }
```

- [ ] **Step 2: Run to verify it fails**

Run: `mvn -pl BE verify -Dit.test=PetFlowIT -DfailIfNoTests=false`
Expected: FAIL trước khi Task 1-5 xong; sau khi xong phải PASS (implementer Task 6 chạy sau Task 5 nên bước này là confirm đỏ→xanh).

- [ ] **Step 3: Fix tối thiểu để xanh** (không thêm scope; chỉ sửa wiring/security nếu IT đỏ).

- [ ] **Step 4: Run full suite**

Run: `mvn -pl BE test` rồi `mvn -pl BE verify`
Expected: PASS toàn suite (unit + slice + IT cũ `AuthFlowIT/RefreshTokenServiceIT` + `PetFlowIT`).

- [ ] **Step 5: Commit**

```bash
git add BE/src/test/java/com/petcare/module/pet/PetFlowIT.java
git commit -m "test(pet): add PetFlowIT owner-guard E2E"
```

---

## Docs cập nhật (gộp vào Task 5, không task riêng)

- `docs/api/customer-pet-v1.md §C1-C4`: đánh dấu `transfer/search/caregiver = OUT v1`; sửa `PetResponse.isActive:boolean → status: PetStatus enum` (hiện lạc hậu so với RULE-04-11/ERD).
- Không đụng `00-requirements.md (FROZEN)`.

## Verification Checklist

- [ ] Mọi op có API: AddPet→POST, ViewPet→GET, UpdatePet→PATCH; guard owner mọi endpoint.
- [ ] RULE-04-01/03 enforce ở Service cùng tx; RULE-04-11 rút gọn (khóa status).
- [ ] Không trả Entity; envelope `ApiResponse/PageResponse/ErrorResponse` thống nhất.
- [ ] 403 cross-owner, 404 missing, 400 validation/RULE, 409 concurrency.
- [ ] `mvn test` + `mvn verify` xanh; ponytail: không file/abstraction thừa.
